{
  "name": "trading_bot_desktop",
  "version": "1.0.0",
  "private": true,
  "main": "app.js",
  "scripts": {
    "build": "ncc build app.js -o dist/ && pkg dist/index.js --out-path dist/ && rm dist/index.js"
  },
  "dependencies": {
    "@alphainsider/trading_bot": "file:.."
  },
  "devDependencies": {
    "@vercel/ncc": "^0.36.1",
    "pkg": "^5.8.1"
  }
}
